name: docs-locale-sync

on:
  workflow_dispatch:

jobs:
  sync-locales:
    if: github.repository == 'SymbolicOS/symbolic'
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Skip deprecated locale sync
        run: |
          echo "`packages/web-legacy` is deprecated."
          echo "Locale sync stays disabled until a replacement docs surface is chosen."
