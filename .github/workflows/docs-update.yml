name: docs-update

on:
  workflow_dispatch:

env:
  LOOKBACK_HOURS: 4

jobs:
  update-docs:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Skip deprecated docs automation
        run: |
          echo "`packages/web-legacy` is deprecated."
          echo "Automatic docs updates stay disabled until a replacement docs surface is chosen."
