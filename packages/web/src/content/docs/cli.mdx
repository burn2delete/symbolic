---
title: CLI
description: Symbolic CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The Symbolic CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
symbolic
```

But it also accepts commands as documented on this page. This allows you to interact with Symbolic programmatically.

```bash
symbolic run "Explain how closures work in JavaScript"
```

---

### tui

Start the Symbolic terminal user interface.

```bash
symbolic [project]
```

#### Flags

| Flag         | Short | Description                                                             |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--continue` | `-c`  | Continue the last session                                               |
| `--session`  | `-s`  | Session ID to continue                                                  |
| `--fork`     |       | Fork the session when continuing (use with `--continue` or `--session`) |
| `--prompt`   |       | Prompt to use                                                           |
| `--model`    | `-m`  | Model to use in the form of provider/model                              |
| `--agent`    |       | Agent to use                                                            |
| `--port`     |       | Port to listen on                                                       |
| `--hostname` |       | Hostname to listen on                                                   |

---

## Commands

The Symbolic CLI also has the following commands.

---

### agent

Manage agents for Symbolic.

```bash
symbolic agent [command]
```

---

### attach

Attach a terminal to an already running Symbolic backend server started via `serve` or `web` commands.

```bash
symbolic attach [url]
```

This allows using the TUI with a remote Symbolic backend. For example:

```bash
# Start the backend server for web/mobile access
symbolic web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
symbolic attach http://10.20.30.40:4096
```

#### Flags

| Flag        | Short | Description                       |
| ----------- | ----- | --------------------------------- |
| `--dir`     |       | Working directory to start TUI in |
| `--session` | `-s`  | Session ID to continue            |

---

#### create

Create a new agent with custom configuration.

```bash
symbolic agent create
```

This command will guide you through creating a new agent with a custom system prompt and tool configuration.

---

#### list

List all available agents.

```bash
symbolic agent list
```

---

### auth

Command to manage credentials and login for providers.

```bash
symbolic auth [command]
```

---

#### login

Symbolic is powered by the provider list at [Models.dev](https://models.dev), so you can use `symbolic auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/symbolic/auth.json`.

```bash
symbolic auth login
```

When Symbolic starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
symbolic auth list
```

Or the short version.

```bash
symbolic auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
symbolic auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
symbolic github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
symbolic github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
symbolic github run
```

##### Flags

| Flag      | Description                            |
| --------- | -------------------------------------- |
| `--event` | GitHub mock event to run the agent for |
| `--token` | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
symbolic mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
symbolic mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
symbolic mcp list
```

Or use the short version.

```bash
symbolic mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
symbolic mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
symbolic mcp auth list
```

Or use the short version.

```bash
symbolic mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
symbolic mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
symbolic mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
symbolic models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
symbolic models anthropic
```

#### Flags

| Flag        | Description                                                  |
| ----------- | ------------------------------------------------------------ |
| `--refresh` | Refresh the models cache from models.dev                     |
| `--verbose` | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in Symbolic.

```bash
symbolic models --refresh
```

---

### run

Run symbolic in non-interactive mode by passing a prompt directly.

```bash
symbolic run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "symbolic run"
symbolic run Explain the use of context in Go
```

You can also attach to a running `symbolic serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
symbolic serve

# In another terminal, run commands that attach to it
symbolic run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag         | Short | Description                                                             |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--command`  |       | The command to run, use message for args                                |
| `--continue` | `-c`  | Continue the last session                                               |
| `--session`  | `-s`  | Session ID to continue                                                  |
| `--fork`     |       | Fork the session when continuing (use with `--continue` or `--session`) |
| `--share`    |       | Share the session                                                       |
| `--model`    | `-m`  | Model to use in the form of provider/model                              |
| `--agent`    |       | Agent to use                                                            |
| `--file`     | `-f`  | File(s) to attach to message                                            |
| `--format`   |       | Format: default (formatted) or json (raw JSON events)                   |
| `--title`    |       | Title for the session (uses truncated prompt if no value provided)      |
| `--attach`   |       | Attach to a running symbolic server (e.g., http://localhost:4096)       |
| `--port`     |       | Port for the local server (defaults to random port)                     |

---

### serve

Start a headless Symbolic server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
symbolic serve
```

This starts an HTTP server that provides API access to symbolic functionality without the TUI interface. Set `SYMBOLIC_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `symbolic`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### session

Manage Symbolic sessions.

```bash
symbolic session [command]
```

---

#### list

List all Symbolic sessions.

```bash
symbolic session list
```

##### Flags

| Flag          | Short | Description                          |
| ------------- | ----- | ------------------------------------ |
| `--max-count` | `-n`  | Limit to N most recent sessions      |
| `--format`    |       | Output format: table or json (table) |

---

### stats

Show token usage and cost statistics for your Symbolic sessions.

```bash
symbolic stats
```

#### Flags

| Flag        | Description                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--days`    | Show stats for the last N days (all time)                                   |
| `--tools`   | Number of tools to show (all)                                               |
| `--models`  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| `--project` | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
symbolic export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

---

### import

Import session data from a JSON file or Symbolic share URL.

```bash
symbolic import <file>
```

You can import from a local file or an Symbolic share URL.

```bash
symbolic import session.json
symbolic import https://symbolic.computer/s/abc123
```

---

### web

Start a headless Symbolic server with a web interface.

```bash
symbolic web
```

This starts an HTTP server and opens a web browser to access Symbolic through a web interface. Set `SYMBOLIC_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `symbolic`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
symbolic acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag         | Description           |
| ------------ | --------------------- |
| `--cwd`      | Working directory     |
| `--port`     | Port to listen on     |
| `--hostname` | Hostname to listen on |

---

### uninstall

Uninstall Symbolic and remove all related files.

```bash
symbolic uninstall
```

#### Flags

| Flag            | Short | Description                                 |
| --------------- | ----- | ------------------------------------------- |
| `--keep-config` | `-c`  | Keep configuration files                    |
| `--keep-data`   | `-d`  | Keep session data and snapshots             |
| `--dry-run`     |       | Show what would be removed without removing |
| `--force`       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates symbolic to the latest version or a specific version.

```bash
symbolic upgrade [target]
```

To upgrade to the latest version.

```bash
symbolic upgrade
```

To upgrade to a specific version.

```bash
symbolic upgrade v0.1.48
```

#### Flags

| Flag       | Short | Description                                                       |
| ---------- | ----- | ----------------------------------------------------------------- |
| `--method` | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The symbolic CLI takes the following global flags.

| Flag           | Short | Description                          |
| -------------- | ----- | ------------------------------------ |
| `--help`       | `-h`  | Display help                         |
| `--version`    | `-v`  | Print version number                 |
| `--print-logs` |       | Print logs to stderr                 |
| `--log-level`  |       | Log level (DEBUG, INFO, WARN, ERROR) |

---

## Environment variables

Symbolic can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `SYMBOLIC_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `SYMBOLIC_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `SYMBOLIC_CONFIG`                     | string  | Path to config file                               |
| `SYMBOLIC_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `SYMBOLIC_CONFIG_DIR`                 | string  | Path to config directory                          |
| `SYMBOLIC_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `SYMBOLIC_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `SYMBOLIC_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `SYMBOLIC_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `SYMBOLIC_PERMISSION`                 | string  | Inlined json permissions config                   |
| `SYMBOLIC_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `SYMBOLIC_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `SYMBOLIC_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `SYMBOLIC_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `SYMBOLIC_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `SYMBOLIC_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `SYMBOLIC_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `SYMBOLIC_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `SYMBOLIC_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `SYMBOLIC_DISABLE_FILETIME_CHECK`     | boolean | Disable file time checking for optimization       |
| `SYMBOLIC_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `SYMBOLIC_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `SYMBOLIC_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `SYMBOLIC_SERVER_USERNAME`            | string  | Override basic auth username (default `symbolic`) |
| `SYMBOLIC_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `SYMBOLIC_EXPERIMENTAL`                         | boolean | Enable all experimental features        |
| `SYMBOLIC_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `SYMBOLIC_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `SYMBOLIC_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `SYMBOLIC_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `SYMBOLIC_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `SYMBOLIC_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `SYMBOLIC_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `SYMBOLIC_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `SYMBOLIC_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `SYMBOLIC_EXPERIMENTAL_LSP_TY`                  | boolean | Enable experimental LSP type checking   |
| `SYMBOLIC_EXPERIMENTAL_MARKDOWN`                | boolean | Enable experimental markdown features   |
| `SYMBOLIC_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
